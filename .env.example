# Frontend Environment Variables
# Copy this file to .env.local and update values as needed
#
# For Vite, variables must be prefixed with VITE_ to be exposed to the frontend

# =============================================================================
# Supabase Configuration (Self-Hosted)
# =============================================================================
# URL of your Supabase instance (Kong gateway)
VITE_SUPABASE_URL=http://localhost:8000

# Supabase Anonymous Key (public, safe to share)
# Get this from your supabase-docker/.env file (ANON_KEY)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# Backend API Configuration
# =============================================================================
# URL of the FastAPI backend server
VITE_API_BASE_URL=http://localhost:8001
